# 📊 HƯỚNG DẪN LƯU TRỮ VÀ LUỒNG DỮ LIỆU

## 🎯 TỔNG QUAN

Hệ thống eLearning sử dụng **Multi-Layer Storage Strategy** với 3 tầng lưu trữ:

```
┌─────────────────────────────────────────────────────────┐
│  CONTENT DATA (Books, Lessons, Quizzes, Exams)          │
│  ┌──────────────────┐  ┌──────────────────┐            │
│  │   IndexedDB      │  │   localStorage    │            │
│  │   (Primary)      │  │   (Fallback)     │            │
│  └──────────────────┘  └──────────────────┘            │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│  USER DATA (Progress, Exam Results, Settings)           │
│  ┌──────────────────┐  ┌──────────────────┐            │
│  │   Supabase       │  │   localStorage    │            │
│  │   (Cloud)        │  │   (Local Cache)   │            │
│  └──────────────────┘  └──────────────────┘            │
└─────────────────────────────────────────────────────────┘
```

---

## 📍 1. CONTENT DATA (Nội dung học tập)

### **A. Nơi lưu trữ:**

#### **1. IndexedDB (Primary - Ưu tiên)**
- **Database:** `elearning-db` (Version 2)
- **Vị trí:** Trình duyệt của user
- **Dung lượng:** Không giới hạn
- **Tốc độ:** Nhanh, offline support

**Cấu trúc:**
```
elearning-db
├── books          → Danh sách sách (theo level)
├── series         → Danh sách series (theo level)
├── chapters       → Chapters của từng book
├── lessons        → Lessons của từng chapter
├── quizzes        → Quiz của từng lesson
└── exams          → JLPT exams (theo level)
```

**Vị trí trên máy:**
- **Windows Chrome:** `C:\Users\[User]\AppData\Local\Google\Chrome\User Data\Default\IndexedDB\`
- **Windows Firefox:** `C:\Users\[User]\AppData\Roaming\Mozilla\Firefox\Profiles\[Profile]\storage\default\`

#### **2. localStorage (Fallback)**
- **Khi nào dùng:** Nếu IndexedDB không khả dụng
- **Dung lượng:** 5-10MB (giới hạn)
- **Key pattern:**
  - `elearning_books_n1` → Books level N1
  - `elearning_chapters_[bookId]` → Chapters
  - `elearning_lessons_[bookId]_[chapterId]` → Lessons
  - `elearning_quiz_[bookId]_[chapterId]_[lessonId]` → Quiz

#### **3. Static Files (Default)**
- **Khi nào dùng:** Nếu không có trong IndexedDB/localStorage
- **Vị trí:** Code trong `src/data/` hoặc `src/features/`
- **Mục đích:** Demo data, fallback

---

### **B. Data Flow - Content Data:**

```
┌─────────────────────────────────────────────────────────┐
│  1. ADMIN NHẬP DỮ LIỆU                                  │
│     (ContentManagementPage)                             │
└──────────────────┬──────────────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────────────┐
│  2. LƯU VÀO STORAGE                                     │
│     storageManager.saveBooks()                          │
│     storageManager.saveChapters()                        │
│     storageManager.saveLessons()                         │
│     storageManager.saveQuiz()                            │
└──────────────────┬──────────────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────────────┐
│  3. STORAGE MANAGER QUYẾT ĐỊNH                           │
│     ┌─────────────────────────────────────┐            │
│     │ Try IndexedDB first                 │            │
│     │   → If available: Save to IndexedDB │            │
│     │   → If not: Fallback to localStorage│            │
│     └─────────────────────────────────────┘            │
└──────────────────┬──────────────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────────────┐
│  4. DỮ LIỆU ĐƯỢC LƯU                                    │
│     ✅ IndexedDB: elearning-db                           │
│     ✅ localStorage: (nếu IndexedDB fail)               │
└─────────────────────────────────────────────────────────┘
```

---

### **C. Data Flow - Load Content:**

```
┌─────────────────────────────────────────────────────────┐
│  COMPONENT CẦN DỮ LIỆU                                   │
│  (BookDetailPage, LessonPage, QuizPage)                 │
└──────────────────┬──────────────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────────────┐
│  storageManager.getBooks()                               │
│  storageManager.getChapters()                            │
│  storageManager.getLessons()                             │
│  storageManager.getQuiz()                                │
└──────────────────┬──────────────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────────────┐
│  STORAGE MANAGER TÌM KIẾM (Priority Order):             │
│                                                          │
│  1️⃣ Try IndexedDB first                                 │
│     → If found: Return data                             │
│                                                          │
│  2️⃣ Fallback to localStorage                            │
│     → If found: Return data + Sync to IndexedDB         │
│                                                          │
│  3️⃣ Fallback to static files                           │
│     → If found: Return demo data                       │
└──────────────────┬──────────────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────────────┐
│  COMPONENT NHẬN DỮ LIỆU                                 │
│  → Hiển thị cho user                                    │
└─────────────────────────────────────────────────────────┘
```

---

## 📍 2. USER DATA (Dữ liệu người dùng)

### **A. Nơi lưu trữ:**

#### **1. Supabase (Cloud - Primary)**
- **Vị trí:** Server trên cloud (Supabase PostgreSQL)
- **Dữ liệu:**
  - ✅ Exam Results (`exam_results` table)
  - ✅ Learning Progress (`learning_progress` table)
  - ✅ User Profiles (`profiles` table)
  - ✅ App Settings (`app_settings` table)

**Tính chất:**
- ✅ Sync đa thiết bị (tự động)
- ✅ Backup tự động (server)
- ✅ An toàn (RLS policies)
- ⚠️ Cần internet

#### **2. localStorage (Local Cache)**
- **Vị trí:** Trình duyệt của user
- **Dữ liệu:**
  - ✅ Exam answers (`exam-[level]-[examId]-knowledge`)
  - ✅ Exam scores (`exam-[level]-[examId]-knowledge-score`)
  - ✅ Lesson completion (`lesson_completed_[bookId]_[chapterId]_[lessonId]`)
  - ✅ Quiz scores (`quiz_scores_[bookId]_[chapterId]_[lessonId]`)

**Tính chất:**
- ✅ Offline support
- ✅ Fast access
- ⚠️ Chỉ trên thiết bị hiện tại
- ⚠️ Có thể bị mất nếu xóa browser data

---

### **B. Data Flow - User Progress:**

```
┌─────────────────────────────────────────────────────────┐
│  USER LÀM BÀI (Exam, Quiz, Lesson)                        │
└──────────────────┬──────────────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────────────┐
│  LƯU VÀO LOCALSTORAGE (Immediate)                       │
│  → Fast, offline support                                │
└──────────────────┬──────────────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────────────┐
│  NẾU USER ĐÃ ĐĂNG NHẬP (Supabase)                        │
│     ↓                                                    │
│  LƯU VÀO SUPABASE (Sync)                                │
│  → examResultsService.saveExamResult()                  │
│  → learningProgressService.saveLearningProgress()       │
└──────────────────┬──────────────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────────────┐
│  DỮ LIỆU ĐƯỢC LƯU Ở 2 NƠI:                              │
│  ✅ localStorage (local cache)                          │
│  ✅ Supabase (cloud backup)                             │
└─────────────────────────────────────────────────────────┘
```

---

### **C. Data Flow - Load User Progress:**

```
┌─────────────────────────────────────────────────────────┐
│  COMPONENT CẦN USER PROGRESS                             │
│  (UserDashboard, LessonPage, QuizPage)                   │
└──────────────────┬──────────────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────────────┐
│  KIỂM TRA USER ĐÃ ĐĂNG NHẬP?                            │
│  ┌─────────────────────────────────────┐               │
│  │ ✅ YES (Supabase user)               │               │
│  │   → Load from Supabase               │               │
│  │   → examResultsService.getUserExamResults()          │
│  │   → learningProgressService.getUserProgress()        │
│  │                                        │               │
│  │ ❌ NO (Guest user)                    │               │
│  │   → Load from localStorage            │               │
│  │   → localStorage.getItem('exam-...')  │               │
│  └─────────────────────────────────────┘               │
└──────────────────┬──────────────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────────────┐
│  COMPONENT NHẬN DỮ LIỆU                                 │
│  → Hiển thị progress cho user                           │
└─────────────────────────────────────────────────────────┘
```

---

## 🔄 3. DATA SYNCHRONIZATION

### **A. Auto Sync (Tự động):**

```
┌─────────────────────────────────────────────────────────┐
│  USER ĐĂNG NHẬP VỚI SUPABASE                             │
└──────────────────┬──────────────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────────────┐
│  AUTO SYNC TRIGGER                                       │
│  → dataSyncService.fullSync(userId)                     │
└──────────────────┬──────────────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────────────┐
│  STEP 1: BACKUP (localStorage → Supabase)               │
│  → Scan localStorage                                    │
│  → Save to Supabase (nếu chưa có)                       │
└──────────────────┬──────────────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────────────┐
│  STEP 2: RESTORE (Supabase → localStorage)              │
│  → Load from Supabase                                   │
│  → Save to localStorage                                  │
│  → Conflict resolution: Supabase wins                  │
└─────────────────────────────────────────────────────────┘
```

### **B. Manual Sync (Thủ công):**

```
┌─────────────────────────────────────────────────────────┐
│  USER CLICK "🔄 Sync dữ liệu"                            │
│  (DataSyncButton component)                             │
└──────────────────┬──────────────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────────────┐
│  OPTIONS:                                                │
│  ┌─────────────────────────────────────┐               │
│  │ 1. Full Sync                        │               │
│  │    → Backup + Restore               │               │
│  │                                      │               │
│  │ 2. Backup Only                      │               │
│  │    → localStorage → Supabase        │               │
│  │                                      │               │
│  │ 3. Restore Only                     │               │
│  │    → Supabase → localStorage        │               │
│  └─────────────────────────────────────┘               │
└─────────────────────────────────────────────────────────┘
```

---

## 📊 4. COMPLETE DATA FLOW DIAGRAM

```
┌─────────────────────────────────────────────────────────────┐
│                    ADMIN NHẬP DỮ LIỆU                        │
│              (ContentManagementPage)                         │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
        ┌────────────────────────────────────┐
        │   storageManager.saveX()          │
        └────────────┬───────────────────────┘
                     │
        ┌────────────┴────────────┐
        │                         │
        ▼                         ▼
┌───────────────┐         ┌───────────────┐
│  IndexedDB    │         │ localStorage  │
│  (Primary)    │         │ (Fallback)    │
└───────────────┘         └───────────────┘
        │                         │
        └────────────┬────────────┘
                     │
                     ▼
        ┌────────────────────────┐
        │  CONTENT DATA STORED    │
        │  (Books, Lessons, etc.) │
        └────────────────────────┘


┌─────────────────────────────────────────────────────────────┐
│                    USER LÀM BÀI                             │
│         (Exam, Quiz, Lesson Completion)                     │
└────────────────────────┬────────────────────────────────────┘
                         │
        ┌────────────────┴────────────────┐
        │                                  │
        ▼                                  ▼
┌───────────────┐                 ┌───────────────┐
│ localStorage  │                 │   Supabase    │
│ (Immediate)   │                 │   (If logged) │
└───────────────┘                 └───────────────┘
        │                                  │
        └────────────┬─────────────────────┘
                     │
                     ▼
        ┌────────────────────────┐
        │  USER PROGRESS STORED  │
        │  (Results, Scores)     │
        └────────────────────────┘


┌─────────────────────────────────────────────────────────────┐
│                    COMPONENT LOAD DATA                       │
│         (BookDetailPage, LessonPage, UserDashboard)          │
└────────────────────────┬────────────────────────────────────┘
                         │
        ┌────────────────┴────────────────┐
        │                                  │
        ▼                                  ▼
┌───────────────┐                 ┌───────────────┐
│  CONTENT DATA │                 │  USER DATA    │
│               │                 │               │
│  Priority:    │                 │  Priority:    │
│  1. IndexedDB │                 │  1. Supabase │
│  2. localStorage│                │  2. localStorage│
│  3. Static    │                 │               │
└───────────────┘                 └───────────────┘
        │                                  │
        └────────────┬─────────────────────┘
                     │
                     ▼
        ┌────────────────────────┐
        │  DATA DISPLAYED        │
        │  (To User)             │
        └────────────────────────┘
```

---

## 🎯 TÓM TẮT

### **Content Data (Nội dung học tập):**
- **Lưu ở:** IndexedDB (primary) → localStorage (fallback)
- **Truyền cho hệ thống:** Qua `storageManager` service
- **Tính chất:** Local, có thể edit/delete, không sync đa thiết bị

### **User Data (Dữ liệu người dùng):**
- **Lưu ở:** Supabase (cloud) + localStorage (cache)
- **Truyền cho hệ thống:** Qua `examResultsService`, `learningProgressService`
- **Tính chất:** Sync đa thiết bị, backup tự động, an toàn

### **Data Flow:**
1. **Admin nhập** → Lưu vào IndexedDB/localStorage
2. **User làm bài** → Lưu vào localStorage + Supabase (nếu logged in)
3. **Component load** → Tìm trong IndexedDB → localStorage → Supabase → Static
4. **Auto sync** → Khi user login, sync giữa localStorage ↔ Supabase

---

## 📚 TÀI LIỆU THAM KHẢO

- `docs/data/DATA_STORAGE_AND_PERSISTENCE.md` - Chi tiết về storage
- `docs/data/DATA_FLOW_DOCUMENTATION.md` - Luồng xử lý dữ liệu
- `docs/data/DATA_SYNC_GUIDE.md` - Hướng dẫn sync
- `docs/api/SUPABASE_API_REFERENCE.md` - API reference

---

**Tóm lại: Dữ liệu được lưu ở IndexedDB (content) và Supabase (user data), truyền cho hệ thống qua các service managers (storageManager, examResultsService, learningProgressService).**

