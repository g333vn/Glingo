# 📊 TÀI LIỆU LUỒNG XỬ LÝ DỮ LIỆU

## 🎯 TỔNG QUAN

Ứng dụng eLearning sử dụng kiến trúc **Multi-layer Storage Strategy** với 3 tầng lưu trữ:
1. **IndexedDB** (Primary) - Dung lượng không giới hạn
2. **localStorage** (Fallback) - Dung lượng 5-10MB
3. **Static Files** (Default) - Dữ liệu mặc định trong code

---

## 🏗️ KIẾN TRÚC LƯU TRỮ

### 1. Storage Manager (`localStorageManager.js`)

**Vai trò:** Quản lý thống nhất tất cả các thao tác lưu trữ

**Chiến lược:**
```
┌─────────────────────────────────────────┐
│  Component Request Data                  │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│  localStorageManager.getX()              │
│  ┌───────────────────────────────────┐   │
│  │ 1. Try IndexedDB first           │   │
│  │    → If found: Return + Sync      │   │
│  │ 2. Fallback to localStorage       │   │
│  │    → If found: Return + Sync      │   │
│  │ 3. Fallback to static file        │   │
│  └───────────────────────────────────┘   │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│  Return Data to Component               │
└─────────────────────────────────────────┘
```

**Ví dụ:**
```javascript
// Load exam data
const savedExam = await storageManager.getExam(levelId, examId);
if (savedExam) {
  // Data từ IndexedDB hoặc localStorage
  setExamData(savedExam);
} else {
  // Fallback to static file
  const staticData = getListeningQuestions(levelId, examId);
  setExamData(staticData);
}
```

### 2. IndexedDB Manager (`indexedDBManager.js`)

**Vai trò:** Quản lý IndexedDB với các object stores:
- `books` - Sách theo level
- `series` - Series sách
- `chapters` - Chương sách
- `quizzes` - Câu hỏi quiz
- `exams` - Đề thi JLPT
- `levelConfigs` - Cấu hình level

**Key Structure:**
- Books: `{ level, id }` as keyPath
- Exams: `{ level, examId }` as keyPath
- Quizzes: `{ bookId, chapterId }` as keyPath

---

## 🔄 LUỒNG XỬ LÝ DỮ LIỆU CHI TIẾT

### A. LUỒNG LOAD DỮ LIỆU (READ)

#### 1. **Load Exam Data (ExamListeningPage)**

```
┌─────────────────────────────────────────────────┐
│  User navigates to /jlpt/n1/2024-12/listening  │
└──────────────┬──────────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────────┐
│  useEffect(() => {                              │
│    loadExamData()                               │
│  }, [levelId, examId])                          │
└──────────────┬──────────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────────┐
│  Step 1: Try storageManager.getExam()          │
│  ├─ Check IndexedDB                            │
│  │  └─ If found: Transform & Return            │
│  └─ Check localStorage                         │
│     └─ If found: Transform & Sync to IndexedDB │
└──────────────┬──────────────────────────────────┘
               │
               ▼ (If not found)
┌─────────────────────────────────────────────────┐
│  Step 2: Fallback to Static File               │
│  └─ getListeningQuestions(levelId, examId)     │
└──────────────┬──────────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────────┐
│  setExamData(data)                             │
│  └─ Component renders with data                │
└─────────────────────────────────────────────────┘
```

**Code thực tế:**
```javascript
// src/features/jlpt/pages/ExamListeningPage.jsx:275-325
useEffect(() => {
  const loadExamData = async () => {
    setIsLoading(true);
    try {
      // 1. Try storage first (admin created exams)
      const savedExam = await storageManager.getExam(levelId, examId);
      if (savedExam && savedExam.listening && savedExam.listening.sections) {
        // Transform to match expected format
        const transformedData = { /* ... */ };
        setExamData(transformedData);
        return;
      }

      // 2. Fallback to static file
      const staticData = getListeningQuestions(levelId, examId);
      if (staticData) {
        setExamData(staticData);
      }
    } catch (error) {
      // Error handling + fallback
    } finally {
      setIsLoading(false);
    }
  };
  loadExamData();
}, [levelId, examId]);
```

#### 2. **Load Quiz Data (QuizPage)**

```
┌─────────────────────────────────────────────────┐
│  User navigates to /level/n1/book/lesson        │
└──────────────┬──────────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────────┐
│  useEffect(() => {                              │
│    loadQuiz()                                   │
│  }, [bookId, lessonId])                         │
└──────────────┬──────────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────────┐
│  Step 1: storageManager.getQuiz(bookId, lessonId)│
│  ├─ IndexedDB (unlimited storage)              │
│  └─ localStorage (fallback)                     │
└──────────────┬──────────────────────────────────┘
               │
               ▼ (If not found)
┌─────────────────────────────────────────────────┐
│  Step 2: Load từ JSON file                      │
│  └─ Import từ static quiz data                 │
└──────────────┬──────────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────────┐
│  setCurrentQuiz(quizData)                       │
└─────────────────────────────────────────────────┘
```

#### 3. **Load Books Metadata (LevelN1Page)**

```
┌─────────────────────────────────────────────────┐
│  User navigates to /level/n1                   │
└──────────────┬──────────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────────┐
│  useEffect(() => {                              │
│    const savedBooks = localStorage.getItem(     │
│      'adminBooks_n1'                            │
│    );                                           │
│    if (savedBooks) {                            │
│      setN1Books(JSON.parse(savedBooks));        │
│    } else {                                     │
│      setN1Books(n1BooksMetadata); // Static     │
│    }                                            │
│  }, []);                                        │
└─────────────────────────────────────────────────┘
```

### B. LUỒNG LƯU DỮ LIỆU (WRITE)

#### 1. **Save Exam Data (Admin)**

```
┌─────────────────────────────────────────────────┐
│  Admin creates/edits exam in ExamManagementPage │
└──────────────┬──────────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────────┐
│  storageManager.saveExam(level, examId, data)  │
└──────────────┬──────────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────────┐
│  Step 1: Save to IndexedDB (PRIMARY)            │
│  └─ indexedDBManager.saveExam()               │
│     └─ Store in 'exams' objectStore            │
└──────────────┬──────────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────────┐
│  Step 2: Sync to localStorage (BACKUP)         │
│  └─ localStorage.setItem()                      │
│     └─ Key: 'adminExam_${level}_${examId}'      │
└──────────────┬──────────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────────┐
│  Success: Data available for both              │
│  - IndexedDB: Unlimited capacity               │
│  - localStorage: Quick access backup             │
└─────────────────────────────────────────────────┘
```

**Code thực tế:**
```javascript
// src/utils/localStorageManager.js:552-587
async saveExam(level, examId, examData) {
  // Save to IndexedDB (primary)
  if (this.useIndexedDB) {
    const success = await indexedDBManager.saveExam(level, examId, examData);
    if (success) {
      // Also save to localStorage if possible
      if (this.storageAvailable) {
        try {
          const key = `adminExam_${level}_${examId}`;
          localStorage.setItem(key, JSON.stringify(examData));
        } catch (e) {
          console.warn('localStorage full, but exam saved to IndexedDB');
        }
      }
      return true;
    }
  }
  // Fallback to localStorage only
  // ...
}
```

#### 2. **Save Quiz Data (Admin)**

```
┌─────────────────────────────────────────────────┐
│  Admin creates quiz in QuizEditorPage           │
│  └─ 50 questions/chapter (large data)          │
└──────────────┬──────────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────────┐
│  storageManager.saveQuiz(bookId, chapterId,    │
│                         quizData)               │
└──────────────┬──────────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────────┐
│  Step 1: IndexedDB (CRITICAL for large data!)  │
│  └─ Can store 250MB+ without issues           │
└──────────────┬──────────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────────┐
│  Step 2: Try localStorage (may fail if full)   │
│  └─ If QuotaExceededError: OK, IndexedDB has it│
└─────────────────────────────────────────────────┘
```

### C. LUỒNG XỬ LÝ EXAM (User Flow)

#### 1. **Exam Navigation Flow**

```
┌─────────────────────────────────────────────────┐
│  Detail Page (/jlpt/n1/2024-12)                │
│  └─ Status: Chưa làm gì                        │
└──────────────┬──────────────────────────────────┘
               │
               ▼ (Click "Bắt đầu Knowledge")
┌─────────────────────────────────────────────────┐
│  useExamGuard: shouldShowWarning()             │
│  └─ Exception: Same exam Knowledge → Allow     │
└──────────────┬──────────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────────┐
│  Knowledge Page (/knowledge)                    │
│  └─ Status: In progress                         │
│  └─ Guard: Active (warn if leave)              │
└──────────────┬──────────────────────────────────┘
               │
               ▼ (Submit)
┌─────────────────────────────────────────────────┐
│  Save to localStorage:                          │
│  - exam-{level}-{examId}-knowledge             │
│  - exam-{level}-{examId}-knowledge-score       │
│  - exam-{level}-{examId}-knowledge-completed   │
└──────────────┬──────────────────────────────────┘
               │
               ▼ (Navigate to Detail)
┌─────────────────────────────────────────────────┐
│  Detail Page                                    │
│  └─ Status: Knowledge done, Listening unlocked │
│  └─ Guard: Active (warn if leave)              │
└──────────────┬──────────────────────────────────┘
               │
               ▼ (Click "Bắt đầu Listening")
┌─────────────────────────────────────────────────┐
│  Listening Page (/listening)                    │
│  └─ Status: In progress                         │
│  └─ Guard: Active                               │
└──────────────┬──────────────────────────────────┘
               │
               ▼ (Submit)
┌─────────────────────────────────────────────────┐
│  Save to localStorage:                          │
│  - exam-{level}-{examId}-listening             │
│  - exam-{level}-{examId}-listening-score       │
│  - exam-{level}-{examId}-listening-completed   │
│  - exam-{level}-{examId}-listening-breakdown    │
└──────────────┬──────────────────────────────────┘
               │
               ▼ (Navigate to Detail)
┌─────────────────────────────────────────────────┐
│  Detail Page                                    │
│  └─ Status: Both completed                     │
│  └─ Show: Result button                        │
└─────────────────────────────────────────────────┘
```

#### 2. **Exam Guard Logic**

**useExamGuard Hook** bảo vệ navigation:

```javascript
// src/hooks/useExamGuard.jsx

// Kiểm tra trạng thái bài thi
getExamStatus() {
  const knowledgeCompleted = localStorage.getItem(
    `exam-${levelId}-${examId}-knowledge-completed`
  ) === 'true';
  
  const listeningCompleted = localStorage.getItem(
    `exam-${levelId}-${examId}-listening-completed`
  ) === 'true';
  
  // Logic xác định khi nào cần cảnh báo
  if (isKnowledgePage && !knowledgeCompleted) {
    return { shouldWarn: true, reason: 'knowledge-in-progress' };
  }
  // ... more cases
}

// Exception: Cho phép navigation trong cùng exam
shouldShowWarning(targetPath) {
  // Exception 1: Detail → Knowledge (same exam)
  if (isNavigatingToSameExamKnowledge(targetPath)) {
    return false; // Allow
  }
  // Exception 2: Detail → Listening (same exam, knowledge done)
  if (isNavigatingToSameExamListening(targetPath)) {
    return false; // Allow
  }
  // ... more exceptions
  
  return status.shouldWarn; // Block if needed
}
```

### D. LUỒNG XỬ LÝ DICTIONARY

#### 1. **Dictionary Initialization**

```
┌─────────────────────────────────────────────────┐
│  App.jsx mounts                                │
└──────────────┬──────────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────────┐
│  useEffect(() => {                              │
│    initJLPTDictionary()                         │
│  }, []);                                        │
└──────────────┬──────────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────────┐
│  dictionaryService.initJLPTDictionary()        │
│  └─ Load từ jlptDictionary.js (8,292 từ)       │
│  └─ Cache vào memory: JLPT_DICT                │
└──────────────┬──────────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────────┐
│  Dictionary ready for use                       │
│  └─ Available globally via context              │
└─────────────────────────────────────────────────┘
```

#### 2. **Word Translation Flow**

```
┌─────────────────────────────────────────────────┐
│  User double-clicks word                         │
└──────────────┬──────────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────────┐
│  dictionaryService.translateToVietnamese(text)  │
└──────────────┬──────────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────────┐
│  Step 1: Check sessionStorage cache             │
│  └─ If cached: Return immediately               │
└──────────────┬──────────────────────────────────┘
               │
               ▼ (Not cached)
┌─────────────────────────────────────────────────┐
│  Step 2: Check JLPT Dictionary (8,292 từ)      │
│  └─ If found: Return Vietnamese meaning         │
└──────────────┬──────────────────────────────────┘
               │
               ▼ (Not found)
┌─────────────────────────────────────────────────┐
│  Step 3: Check Manual Dictionary (200+ từ)      │
│  └─ Common words mapping                        │
└──────────────┬──────────────────────────────────┘
               │
               ▼ (Not found)
┌─────────────────────────────────────────────────┐
│  Step 4: Google Translate API                   │
│  └─ Last resort                                 │
└──────────────┬──────────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────────┐
│  Cache result in sessionStorage                 │
│  └─ Return translation                          │
└─────────────────────────────────────────────────┘
```

### E. LUỒNG XỬ LÝ AUTHENTICATION

```
┌─────────────────────────────────────────────────┐
│  App.jsx                                        │
│  └─ <AuthProvider>                              │
└──────────────┬──────────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────────┐
│  AuthContext mounts                             │
│  └─ useEffect: Load user from localStorage     │
│     └─ Key: 'authUser'                          │
└──────────────┬──────────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────────┐
│  User login                                     │
│  └─ login(username, password)                   │
│     └─ Check against users.js                   │
│     └─ If valid: Save to localStorage          │
└──────────────┬──────────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────────┐
│  Auth state available globally                   │
│  └─ useAuth() hook                              │
│     ├─ user: Current user object                │
│     ├─ isAdmin(): Check admin role              │
│     └─ hasPermission(): Check specific permission│
└─────────────────────────────────────────────────┘
```

---

## 📦 CẤU TRÚC DỮ LIỆU

### 1. **Exam Data Structure**

```javascript
{
  level: 'n1',
  examId: '2024-12',
  title: 'JLPT 2024/12',
  date: '2024/12',
  status: 'Có sẵn',
  imageUrl: '/jlpt/n1/2024-12.jpg',
  knowledge: {
    sections: [
      {
        id: '1',
        title: '文字・語彙',
        questions: [
          {
            id: 1,
            number: '1',
            question: '...',
            options: ['...', '...', '...', '...'],
            correctAnswer: 0,
            explanation: '...'
          }
        ]
      }
    ]
  },
  listening: {
    sections: [
      {
        id: '1',
        title: '問題1',
        instruction: '...',
        timeLimit: 15,
        questions: [
          {
            number: '01',
            audioUrl: '/audio/...',
            options: ['...', '...', '...', '...'],
            correctAnswer: 0,
            explanation: '...'
          }
        ]
      }
    ]
  }
}
```

### 2. **Quiz Data Structure**

```javascript
{
  bookId: 'shinkanzen-n1-bunpou',
  chapterId: 'bai-1',
  title: 'Bài 1',
  questions: [
    {
      id: 1,
      question: '...',
      options: ['...', '...', '...', '...'],
      correctAnswer: 0,
      explanation: '...'
    }
  ]
}
```

### 3. **Answer Storage (localStorage)**

```javascript
// Knowledge answers
'exam-n1-2024-12-knowledge': {
  '1-1': 0,  // section-question: answerIndex
  '1-2': 2,
  // ...
}

// Listening answers
'exam-n1-2024-12-listening': {
  '1-01': 1,
  '1-02': 0,
  // ...
}

// Scores
'exam-n1-2024-12-knowledge-score': 85
'exam-n1-2024-12-listening-score': 90

// Completion status
'exam-n1-2024-12-knowledge-completed': 'true'
'exam-n1-2024-12-listening-completed': 'true'

// Breakdown (for result page)
'exam-n1-2024-12-knowledge-breakdown': {
  vocabulary: 20,
  grammar: 18,
  reading: 22,
  total: 60
}
```

---

## 🔐 BẢO MẬT & TỐI ƯU

### 1. **Storage Strategy**

- **IndexedDB**: Dung lượng không giới hạn, phù hợp cho dữ liệu lớn
- **localStorage**: Backup nhanh, giới hạn 5-10MB
- **Sync**: Tự động sync giữa IndexedDB và localStorage

### 2. **Error Handling**

```javascript
try {
  // Try IndexedDB
  const data = await indexedDBManager.getExam(level, examId);
  if (data) return data;
} catch (error) {
  console.error('IndexedDB error:', error);
  // Fallback to localStorage
}

try {
  // Try localStorage
  const data = localStorage.getItem(key);
  if (data) return JSON.parse(data);
} catch (error) {
  console.error('localStorage error:', error);
  // Fallback to static file
}

// Final fallback
return getStaticData(level, examId);
```

### 3. **Performance Optimization**

- **Caching**: Dictionary cached in memory
- **Lazy Loading**: Data loaded only when needed
- **Batch Operations**: Bulk import/export support
- **IndexedDB Indexes**: Fast queries by level, bookId, etc.

---

## 📝 TÓM TẮT

### Luồng chính:

1. **Load Data**: IndexedDB → localStorage → Static File
2. **Save Data**: IndexedDB (primary) + localStorage (backup)
3. **Exam Flow**: Detail → Knowledge → Listening → Result → Answers
4. **Guard System**: Bảo vệ navigation khi đang làm bài
5. **Dictionary**: Memory cache + JLPT dict + Manual dict + Google Translate

### Điểm mạnh:

✅ **Unlimited Storage**: IndexedDB cho phép lưu trữ không giới hạn  
✅ **Fallback Strategy**: 3 tầng đảm bảo luôn có data  
✅ **Performance**: Caching và lazy loading  
✅ **User Experience**: Guard system bảo vệ tiến độ làm bài  
✅ **Scalability**: Dễ mở rộng với IndexedDB

---

**Tài liệu này mô tả chi tiết luồng xử lý dữ liệu của ứng dụng eLearning.**
